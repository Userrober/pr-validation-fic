{
  "name": "pr-validation-fic-marketplace",
  "owner": {
    "name": "Userrober"
  },
  "metadata": {
    "description": "Standalone marketplace for the pr-validation-fic Claude Code plugin"
  },
  "plugins": [
    {
      "name": "pr-validation-fic",
      "source": "./",
      "description": "Capture BEFORE/AFTER screenshots for SharePoint client PRs on the FIC synthetic tenant — reads PR description + changed files, writes a custom spec per PR (creates pages, seeds REST data, toggles list settings, pulls in a second user as needed), runs it, returns screenshots"
    }
  ]
}
